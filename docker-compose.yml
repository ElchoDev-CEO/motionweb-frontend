services:
  app:
    build: .
    container_name: motion-frontend
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      - ./public/images:/home/app/public/images
